spring:
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true
  security:
    enabled: false
  eureka:
    client:
      enabled: false
      fetchRegistry: false
      registerWithEureka: false

logging:
  level:
    root: INFO
    org.ultra.rcrs.userservice: DEBUG
