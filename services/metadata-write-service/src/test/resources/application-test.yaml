spring:
  application:
    name: metadata-write-service-test
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: true
  security:
    enabled: false

cdn:
  images:
    endpoint: "http://test.localhost:4566"

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
